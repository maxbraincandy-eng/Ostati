# Non-secret development defaults. Real secrets go to .env.local (gitignored).
# Local dev database: docker compose up -d
DATABASE_URL="postgresql://ostati:ostati@localhost:5432/ostati"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-only-secret-change-in-production"
